<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diarydoc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Nearhosp"></activity>
        <activity android:name=".Knowmed" />
        <activity android:name=".AddReminder" />
        <activity android:name=".AddDiagnosis" />
        <activity android:name=".ViewPrescription" />
        <activity android:name=".DoctorHome" />
        <activity android:name=".PatientHome" />
        <activity android:name=".Reminder" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>